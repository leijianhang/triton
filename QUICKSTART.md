# Quickstart

## Start Backend

```bash
cd backend
npm install
npm run dev
```

## Start Frontend

```bash
cd frontend
npm install
npm run dev
```

## Search Symbols

Open the app and use Search Symbol to switch between:

- A-shares
- US stocks
- Hong Kong stocks

The app requests symbol and chart data from `/api/market/:type/*`.
